resources:
  - name: golang_bench
    type: gitRepo
    integration: github-subint
    pointer:
      sourceName: niranjan92/golang_benchmarks
      branch: master
  # This time resource posts new versions for it, every 12 hours
  - name: 12hour-trigger
    type: time
    seed:
      interval: "0 */12 * * *"
